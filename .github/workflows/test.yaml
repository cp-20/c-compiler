name: Test 1cc

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install prerequisites
        run: sudo apt-get install -y clang llvm make

      - name: Test
        run: make test
